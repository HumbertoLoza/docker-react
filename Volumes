 docker run -p 3000:3000  -v $(pwd):/app -v /app/node_modules 2e35

we are creating a mapping between container's fs and our local fs. 

-v src_path:target_path
 
we are creating a mapping between a container and local machine file system

-v container_path

we want to keep that path in the container, don't touch it 
